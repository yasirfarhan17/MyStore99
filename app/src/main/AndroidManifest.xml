<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.noor.mystore99">
    <!--
             The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
             Google Maps Android API v2, but you must specify either coarse or fine
             location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <queries>
        <package android:name="com.google.android.apps.nbu.paisa.user" />
        <package android:name="com.phonepe.app" />
    </queries>

    <application
        android:name=".amigrate.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SbziTaza">
        <activity
            android:name=".amigrate.ui.dashboard.account.myorder.MyOrder"
            android:exported="false" />
        <activity
            android:name=".amigrate.ui.dashboard.account.address.Address"
            android:exported="false" />
        <activity
            android:name=".amigrate.ui.category.CategoryActivity"
            android:exported="false" />
        <activity
            android:name=".amigrate.ui.auth.register.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".amigrate.ui.upi.NewUPIPay"
            android:exported="false" />

        <receiver
            android:name=".amigrate.alarmManager.SetFirebaseDataReciver"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".amigrate.ui.auth.login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".amigrate.ui.checkout.CheckoutActivity"
            android:exported="false" />
        <activity
            android:name=".amigrate.ui.payment.PaymentActivity"
            android:exported="false" />
        <activity android:name=".AboutPage" />
        <activity android:name=".InstructionPage" />
        <activity android:name=".page1" />
        <activity android:name=".signUpPage" />
        <activity android:name=".loginPage" />
        <activity android:name=".first" />
        <activity android:name=".ProfileEdit" />
        <activity android:name=".ProfilePage" />
        <activity android:name=".OrderPage" />
        <activity android:name=".PaymentOption" />
        <activity android:name=".Address1" />
        <activity android:name=".upiPay" />
        <!--
                 The API key for Google Maps-based APIs is defined as a string resource.
                 (See the file "res/values/google_maps_api.xml").
                 Note that the API key is linked to the encryption key used to sign the APK.
                 You need a different API key for each encryption key, including the release key that is used to
                 sign the APK for publishing.
                 You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <activity
            android:name=".amigrate.ui.main.MainActivity"
            android:exported="true"
            android:hardwareAccelerated="true" />
        <activity android:name=".LastPage" />
        <activity android:name=".amigrate.ui.cart.CartActivity" />
        <activity android:name=".CategoryProduct" />
        <activity android:name=".CartProductList" />
        <activity android:name=".AddressPage" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".slpash"
            android:exported="true"
            android:theme="@style/AppTheme">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>

        <service
            android:name=".MyFirebaseMessaging"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>