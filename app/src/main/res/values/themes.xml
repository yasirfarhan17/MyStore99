<resources>

    <style name="Theme.SbziTaza.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.SbziTaza.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.SbziTaza.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>