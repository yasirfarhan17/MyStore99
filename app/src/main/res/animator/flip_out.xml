<?xml version="1.0" encoding="utf-8"?>
<set xmlns:android="http://schemas.android.com/apk/res/android">
    <!--This animation is for the card which is in front and needs to go in back.
    Rotate in y-axis from 0 to 180 angle. Now, remember when we are pushing in back,
     we are bringing back layout in front so if the complete duration of animation is 1500
     then on half time which is 750, change the alpha property from 1 to 0.-->
    <objectAnimator
        android:duration="0"
        android:propertyName="alpha"
        android:valueFrom="1.0"
        android:valueTo="0.0" />
    <objectAnimator
        android:duration="1000"
        android:propertyName="rotationY"
        android:repeatMode="reverse"
        android:valueFrom="180"
        android:valueTo="0" />

    <objectAnimator
        android:duration="0"
        android:propertyName="alpha"
        android:startOffset="500"
        android:valueFrom="0.0"
        android:valueTo="1.0" />

</set>
